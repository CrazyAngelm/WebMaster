# 🗺 Дорожная карта реализации WebMaster (Hornygrad Web)

// 📁 docs/Implementation_Roadmap.md - Навигатор реализации
// 🎯 Core function: Пошаговый план превращения прототипа в полноценную Online RPG
// 💡 Usage: Использовать как основной гайд для разработки и приоритизации задач

Этот документ определяет последовательность шагов для достижения полной готовности проекта. Приоритеты расставлены от критически важных (фундамент) к второстепенным (полировка).

---

## 🚩 Приоритет 1: Серверный авторитет и безопасность (Завершено ✅)
**Цель:** Исключить возможность читерства и перенести "мозги" игры под контроль сервера.

1.  **Миграция логики боя:** (Выполнено)
    *   Создан серверный аналог `CombatEngine` в `server/src/utils/combatEngine.ts`.
    *   Все расчеты вероятностей, попаданий и урона перенесены на Node.js.
2.  **API для действий:** (Выполнено)
    *   Разработаны защищенные эндпоинты в `server/src/controllers/battleController.ts`.
    *   Эндпоинты принимают `battleId`, `attackerId`, `targetId` и `weaponId`, проверяют авторизацию и ход игрока.
3.  **Валидация ресурсов:** (Выполнено)
    *   Сервер проверяет наличие действий и прав на атаку.
4.  **Стейт-машина боя на сервере:** (Выполнено)
    *   Введены таблицы `Battle` и `BattleParticipant` в Prisma.
    *   Сервер хранит состояние боя, очередь ходов и логи.

## 🚩 Приоритет 1.1: Одномерная боевая система (Завершено ✅)
**Цель:** Реализовать упрощенную одномерную систему боя с управлением расстоянием по оси X.

1.  **Одномерная боевая система (Combat Mode):** (Выполнено)
    *   **Переключение режима**: При инициации боя интерфейс переключается в боевой режим с одномерной линией позиционирования.
    *   **Хранение расстояния**: Добавлено в модель `BattleParticipant` поле `distance` (Float, метры) — расстояние от условного центра боевой линии (0 = центр, положительные значения = правая сторона, отрицательные = левая сторона).
    *   **Визуализация боевой линии**: Горизонтальная линия слева направо, на которой отображаются все участники боя. Расстояние между участниками визуально отражает их `distance` друг от друга.
    *   **Расчет расстояния между участниками**: Для каждой пары участников расстояние = `|distance1 - distance2|`. Чем меньше разница, тем ближе визуально расположены спрайты на экране.
    *   **Действие "Движение"**: Реализованы кнопки "Сближение" и "Отступление", а также клик по линии для точного позиционирования. Движение тратит 1 Main Action.
    *   **Дистанция перемещения**: За одно действие персонаж может изменить `distance` на значение `Speed.distancePerAction` (в метрах).
    *   **Валидация движения**: Сервер проверяет, что изменение `distance` не превышает доступную дистанцию за одно действие. Добавлено минимальное расстояние между персонажами (1.5м) для предотвращения наложения спрайтов.
    *   После окончания боя интерфейс возвращается в режим Exploration с 2D-картой.

2.  **Валидация Дальности атак (Server-side):** (Выполнено)
    *   **Формат хранения**: В `ItemTemplate.distance` поддерживается как JSON строка `{ minRange: number, maxRange: number }`, так и legacy строковые категории (`CLOSE`, `MEDIUM`, `FAR`, `SNIPER`).
    *   Оружие и способности получают `minRange` и `maxRange`.
    *   Сервер перед расчетом `resolveAttack` проверяет условие: `minRange <= distance(A, B) <= maxRange`, где `distance(A, B) = |distanceA - distanceB|`.
    *   Группы дистанций (согласно `docs/Боевая система.md`):
        *   `Melee`: 0–5м.
        *   `Medium`: 5–20м.
        *   `Long`: 20–50м.
        *   `Sniper`: 50–200м.

3.  **Механики сдерживания и побега:** (Выполнено)
    *   **Зона контроля**: Область в радиусе 5м вокруг бойца (в одномерном пространстве: `|distance(A, B)| <= 5`).
    *   **Внеочередная атака (Attack of Opportunity)**: Если персонаж пытается удалиться от врага (увеличить `|distance(A, B)|` более чем на 5м за один ход), враг совершает автоматическую атаку.
    *   **Условие побега**: Кнопка "Сбежать" становится активной только при удалении от всех врагов на расстояние > 90м (`|distance(Player, Enemy)| > 90` для всех врагов).

4.  **Визуализация расстояния в бою:** (Выполнено)
    *   При наведении на врага или выборе действия атаки показывается текущее расстояние до цели и доступные диапазоны атаки текущего оружия.
    *   Визуальная индикация: если цель вне досягаемости, кнопка атаки неактивна или подсвечена красным.
    *   При движении показывается визуальная зона досягаемости (оранжевая полоса) и можно кликнуть по линии для точного позиционирования.

## 🚩 Приоритет 2: Глубина боевой системы и Способности
**Цель:** Довести боевку до соответствия `docs/Боевая система.md`.

1.  **Система навыков (Skills):** 
    *   Реализовать механику "подготовки" (Cast Time), когда способность срабатывает через 1-2 хода после активации. 
    *   Добавить систему откатов (Cooldowns), блокирующую повторное использование навыка.
2.  **Движок эффектов (Effects & Buffs):** 
    *   Создать систему "тиков", которая в начале хода каждого участника обсчитывает активные эффекты. 
    *   Добавить поддержку: DoT (урон со временем), HoT (лечение со временем), Stun (пропуск хода), Buff/Debuff (изменение статов).
3.  **Система смерти и воскрешения:** 
    *   Реализовать статус "Downed" при 0 HP. 
    *   Добавить таймер на 3 хода, в течение которых союзники могут "поднять" бойца. 
    *   Создать механику перехода в локацию "Загробный мир" при окончательной смерти.

## 🚩 Приоритет 3: Real-Time Мультиплеер (Online составляющая)
**Цель:** Сделать мир живым и видимым для всех участников.

1.  **Интеграция Socket.io:** 
    *   Настроить сервер для поддержки WebSocket соединений. 
    *   Каждый игрок при входе подключается к "комнате" своей текущей локации.
2.  **Синхронизация мира:** 
    *   Рассылка события "PlayerMoved" всем в комнате, когда кто-то меняет позицию. 
    *   Рассылка "CharacterUpdated", когда кто-то меняет снаряжение, чтобы другие видели актуальный внешний вид.
3.  **Чат-система:** 
    *   Создать каналы сообщений. 
    *   Реализовать фильтрацию: сообщения локального чата видят только игроки в той же `locationId`.
4.  **Наблюдение за боями:** 
    *   Возможность "подключиться" к ID текущего боя как зритель, получая поток логов и анимаций действий участников.

## 🚩 Приоритет 4: AI Storyteller и Динамический контент
**Цель:** Внедрение LLM для создания уникального игрового опыта.

1.  **Интеграция с LLM API:** 
    *   Создать сервис-прослойку для общения с AI (OpenAI/Anthropic). 
    *   Разработать промпты для оценки текстовых описаний действий (тренировок, крафта, RP).
2.  **Генератор динамических ивентов:** 
    *   Система должна уметь генерировать `GameEvent` на лету, основываясь на тегах локации и истории персонажа. 
    *   Результаты выбора в ивентах должны влиять на БД (выдача предметов, изменение статов).
3.  **Улучшенный NPC-интеллект:** 
    *   Дать NPC "память" о прошлых взаимодействиях с игроком. 
    *   Реализовать ветвящиеся диалоги, где доступность реплик зависит от репутации или ранга.

## 🚩 Приоритет 5: Экономика и Социальное развитие
**Цель:** Создание устойчивого игрового цикла (Core Loop).

1.  **Система магазинов и NPC-торговли:** 
    *   Реализовать инвентарь магазина с ограниченным количеством товара. 
    *   Настроить автоматическое обновление ассортимента (ресток) раз в несколько реальных часов.
2.  **Обмен между игроками (P2P Trade):** 
    *   Создать двухстороннее окно обмена, где оба игрока должны нажать "Подтвердить" после любого изменения состава сделки.
3.  **Групповые активности:** 
    *   Добавить тип локации "Dungeon", вход в который возможен только в группе. 
    *   Реализовать механику "общего лута", распределяемого между участниками.
4.  **Рынок (Аукцион):** 
    *   Создать глобальную таблицу лотов, где игроки могут выставлять предметы на продажу за валюту, даже находясь офлайн.

## 🚩 Приоритет 6: Интерфейс, UX и Баланс
**Цель:** Удобство игры и визуальная привлекательность.

1.  **Адаптивность (Mobile Friendly):** 
    *   Переработать интерфейс инвентаря и чата для удобного управления пальцем. 
    *   Использовать выпадающие меню и модальные окна вместо нагромождения кнопок.
2.  **Визуализация бросков:** 
    *   Интегрировать 3D или 2D анимацию кубиков (например, через Three.js или Canvas). 
    *   Добавить визуальные индикаторы "Критический успех" / "Провал".
3.  **Звуковое сопровождение:** 
    *   Звуки ударов, звон монет, эмбиент для разных типов зон (лес, город, кузница).
4.  **Панель Гейм-Мастера:** 
    *   Инструменты для мониторинга активных игроков, ручного редактирования инвентарей и логирования подозрительной активности.

---

## 📈 Критерии успешности (Definition of Done)

### Приоритет 1: Серверный авторитет и безопасность
1. Игрок не может изменить свое HP или монеты через консоль.
2. Все расчеты кубиков подтверждаются сервером.

### Приоритет 1.1: Одномерная боевая система (Завершено ✅)
3. ✅ Участники боя имеют поле `distance` (Float) в `BattleParticipant` для одномерного позиционирования в боевом режиме.
4. ✅ Боевой интерфейс отображает одномерную линию (слева направо) с визуальным размещением участников в зависимости от их `distance`.
5. ✅ Кнопки "Сближение" и "Отступление" в бою корректно изменяют `distance` участника, тратя Main Action и проверяя доступную дистанцию согласно статам. Реализован также клик по линии для точного позиционирования.
6. ✅ Сервер блокирует атаки, если цель находится вне `minRange` или `maxRange` оружия (расстояние = `|distanceA - distanceB|`).
7. ✅ Механика "Внеочередной атаки" срабатывает автоматически при попытке удалиться от врага более чем на 5м за один ход.
8. ✅ При наведении на врага или выборе действия атаки отображается текущее расстояние до цели и доступные диапазоны атаки текущего оружия.

### Приоритет 2: Глубина боевой системы и Способности
9. Способности с Cast Time активируются через указанное количество ходов после использования.
10. Система Cooldowns блокирует повторное использование способности до истечения таймера.
11. Эффекты (DoT, HoT, Stun, Buff/Debuff) корректно применяются в начале каждого хода участника.
12. При достижении 0 HP персонаж переходит в статус "Downed" с таймером на 3 хода.
13. Союзники могут "поднять" бойца в статусе "Downed" в течение таймера.
14. При окончательной смерти персонаж перемещается в локацию "Загробный мир".

### Приоритет 3: Real-Time Мультиплеер
15. Двое игроков в одной комнате видят друг друга и могут торговать/общаться.

### Приоритет 4: AI Storyteller и Динамический контент
16. LLM начисляет бонусы за качественные отыгрыши (Roleplay).

### Приоритет 5: Экономика и Социальное развитие
17. Персонаж корректно прогрессирует от 1 до 5 ранга согласно правилам прорыва.

### Приоритет 6: Интерфейс, UX и Баланс
18. Интерфейс инвентаря и чата адаптирован для мобильных устройств (touch-friendly).
19. Броски кубиков визуализируются с анимацией (3D или 2D) и индикаторами критических результатов.
20. Звуковые эффекты воспроизводятся для ключевых действий (удары, монеты, эмбиент локаций).
21. Панель Гейм-Мастера предоставляет инструменты для мониторинга игроков, редактирования инвентарей и логирования активности.
